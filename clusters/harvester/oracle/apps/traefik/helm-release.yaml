apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: kube-system
spec:
  interval: 5m
  chart:
    spec:
      chart: traefik
      version: v26.0.x
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: flux-system
  valuesFiles:
    - ./resources/traefik-values.yaml
